syntax = "proto3";

package proto;

import "common.proto";

service MasterService {
  rpc Register(RegisterRequest) returns (Empty);
}

message RegisterRequest {
  string ip = 1;
  uint32 port = 2;
}

